/**
 * Phase 2: OpenAPI / Swagger import script placeholder.
 *
 * Usage (after Phase 2 setup):
 * 1. Place your OpenAPI 3.x schema at openapi/openapi.json
 * 2. Install: npm install fumadocs-openapi
 * 3. Run: npm run generate:openapi
 *
 * Example implementation:
 *
 * import { generateFiles } from 'fumadocs-openapi';
 * import { createOpenAPI } from 'fumadocs-openapi/server';
 *
 * const openapi = createOpenAPI({
 *   input: ['./openapi/openapi.json'],
 * });
 *
 * await generateFiles({
 *   input: openapi,
 *   output: './content/docs',
 *   includeDescription: true,
 * });
 */

console.log('OpenAPI import is not enabled in Phase 1.');
console.log('Add openapi/openapi.json and install fumadocs-openapi to enable.');
console.log('See scripts/generate-openapi.ts for setup instructions.');
